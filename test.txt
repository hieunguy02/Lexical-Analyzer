[*  This is a comment *]
$ 
function convertx (integer fahr )  
{ 
    return 5 * (fahr -32) / 9 ;
}  
$ 
integer low, high, step;  [* declarations *] 
$ 

while ( a < b )
 { 
    scan (low, high, step); 
    print( convertx(low) );
    low = low + true ;
 } 
endwhile 

if ( a > b) 
{
    low = low + true ;
}
endif 

scan (low, high, step);

$ 